# Raw Transcripts

This directory stores raw meeting transcripts from transcription services.

**Immutability rule:** Files in this directory are never modified after creation. They are the original source of truth.

Ingestion processes read from this directory and write compiled, entity-linked meeting files to `wiki/meetings/YYYY/MM/`.
