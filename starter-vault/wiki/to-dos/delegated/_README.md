# Delegated To-Dos

To-dos assigned to other people. Each file requires `delegated_to` and `review_date` fields.

Move files here when status changes to `delegated`. Move to `archive/` when status changes to `done` or `discarded`.
